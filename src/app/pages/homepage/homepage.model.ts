import {ToolbarItem} from '../../shared/navbar/navbar.interface';

export class HomepageModel {
    menu: ToolbarItem[] = [
        {
            link: 'admin/unidad',
            text: 'Unidades'
        }
    ];
}
